# YESS frontend

Local dev:

```powershell
cd frontend
npm install
npm run dev
```

Vite dev server will proxy `/api` to `http://127.0.0.1:8000`.

Routes:
- `/login` – phone + OTP flow
- `/partners` – list/search partners, toggle favorites


